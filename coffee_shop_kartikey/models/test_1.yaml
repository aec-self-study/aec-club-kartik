version: 2
 
models:
  - name: product_value
    description: Cleaned up product data for our transactions
    columns:
      - name: order_id
        description: Primary key of the model
        tests:
          - unique
          - not_null